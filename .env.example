PORT=3001
HOST=127.0.0.1
NODE_ENV=development
# Relative paths are resolved from the project working directory.
DB_PATH=server/settlement.db
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001,https://settelment-a881c.web.app,https://settelment-a881c.firebaseapp.com
# Set BOTH before enabling production or any network listener.
# Use a strong unique password. Never copy these values into frontend configuration.
AUTH_USER=
AUTH_PASSWORD=
